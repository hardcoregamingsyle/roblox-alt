apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-default-deny
spec:
  podSelector:
    matchLabels:
      app: api
  policyTypes: [Ingress, Egress]
  ingress: [] # Deny all
  egress:
  - to: [{podSelector: {matchLabels: {app: trusted-backend-service}}}]