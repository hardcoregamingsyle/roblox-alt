apiVersion: v1
kind: LimitRange
metadata:
  name: nexus-limits
  namespace: nexus-engine
spec:
  limits:
  - max: { memory: "1Gi", cpu: "1" }
    min: { memory: "64Mi", cpu: "100m" }
    type: Container