#!/usr/bin/env node
const { execSync } = require('child_process');
try {
    // Audit dependencies
    execSync('pnpm audit --audit-level high');
    // Linting
    execSync('npx eslint .');
    console.log("Pre-commit checks passed. Tests run in CI/CD pipeline.");
} catch (e) {
    process.exit(1);
}